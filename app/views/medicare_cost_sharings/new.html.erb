<div class= "container">
  <section class="content-section marquee">
    <div class="row">
      <div class="col-md-12">
        <h1>Medicare Cost Sharing</h1>
        <p>If you are receiving Medicare through the Social Security Administration, you may qualify for a Medicare cost sharing program that will pay your Medicare Part B premium and -- depending on your income -- your Medicare deductibles and co-pays. </p>
      </div>
    </div>
  </section>

  <section class="content-section">
  <div class="row">
    <div class="col-md-8 col-sm-6 col-xs-12">
    <form action="/medicare_cost_sharings" role="form" method="post">

  <!-- Form for household size -->
    <div class="row">
     <div class="col-md-9 col-sm-10 col-xs-12">
      <label for="household_size">What is your household size? </label>
        <p type="button" data-toggle="popover" data-content="Please include yourself, your spouse, your children under 18 who live with you" data-placement="right" style="border: 0px; color: #4582ec;" class="popover-body">
         <input type="text" id="household_size" name="household_size" placeholder="For example: 3" class="form-field" value="<%= @current_user.try(:household_size) %>"  required>
        </p>
      </div>
    </div>

   <!-- Form for Medicare household -->
    <div class="row">
      <div class="col-md-9 col-sm-10 col-xs-12">
       <label for="medicare_household_size">How many people in your household receive Medicare?</label>
         <input type="text" id="medicare_household_size" name="medicare_household_size" placeholder="For example: 2" class="   form-field"  required>
       </div>
     </div>

    <!-- Form for monthly income -->
      <div class="row">
        <div class="col-md-9 col-sm-10 col-xs-12">
          <label for="assets">What is you gross monthly income?</label>
          <%= render "programs/popover_calculator" %>
            <input type="text" id="monthly_income" name="monthly_income" placeholder="For example: 1000" class="form-field" value="<%= number_with_precision(@current_user.try(:gross_income), precision: 0) %>" required>
        </div>
      </div>

    <!-- Form for assets -->
      <div class="row">
        <div class="col-md-9 col-sm-10 col-xs-12">
          <label for="assets">Please estimate the value of your assets. (Exclude the value of your home and car.)</label>
          <%= render "programs/popover_calculator" %>
            <p type="button" data-toggle="popover" data-content="Personal property is anything owned by a person that is not land or permanently affixed to land. This includes such items as: money in checking and savings accounts;stocks, bonds, savings certificates, and other securities;farm and small business equipment, unless used for income for self-support, estate bequests; and miscellaneous resources that are not real property." data-placement="right" style="border: 0px; color: #4582ec;" class="popover-body">
            <input type="text" id="assets" name="assets" placeholder="For example: 5000" class="form-field" required>
            </p>
        </div>
      </div>

    <!-- Form for disability question -->
      <%= render "programs/zipcode_question" %>

    <!-- Form for location question -->
      <%= render "programs/location_question" %>

    <!-- Form for cellphone question -->
      <%= render "programs/cellphone_question" %>

    <!-- Submit button -->
      <%= render "programs/submit_button" %>

  </form>
  </div>

    <div class="col-md-3 sm-3 xs-12 pull-left">
      <div class="calculator_box">
        <%= render "programs/calculator" %>
      </div>
    </div>

  </div>
  </section>
</div>


