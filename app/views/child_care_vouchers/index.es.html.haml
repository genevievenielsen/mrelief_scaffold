%h1 Listing child_care_vouchers
%table
  %thead
    %tr
      %th Ccdf dependent no
      %th Ccdf eligible children
      %th Ccdf gross income
      %th{:colspan => "3"}
  %tbody
    - @child_care_vouchers.each do |child_care_voucher|
      %tr
        %td= child_care_voucher.ccdf_dependent_no
        %td= child_care_voucher.ccdf_eligible_children
        %td= child_care_voucher.ccdf_gross_income
        %td= link_to 'Show', child_care_voucher
        %td= link_to 'Edit', edit_child_care_voucher_path(child_care_voucher)
        %td= link_to 'Destroy', child_care_voucher, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Child care voucher', new_child_care_voucher_path
