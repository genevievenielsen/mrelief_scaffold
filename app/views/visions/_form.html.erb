<%= form_for(@vision) do |f| %>
  <% if @vision.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vision.errors.count, "error") %> prohibited this vision from being saved:</h2>

      <ul>
      <% @vision.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :vision_gross_income %><br>
    <%= f.text_field :vision_gross_income %>
  </div>
  <div class="field">
    <%= f.label :vision_household_size %><br>
    <%= f.number_field :vision_household_size %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
