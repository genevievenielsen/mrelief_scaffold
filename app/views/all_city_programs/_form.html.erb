<%= form_for(@all_city_program) do |f| %>
  <% if @all_city_program.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@all_city_program.errors.count, "error") %> prohibited this all_city_program from being saved:</h2>

      <ul>
      <% @all_city_program.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
