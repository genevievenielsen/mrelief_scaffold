= form_for(@snap_eligibility) do |f|
  - if @snap_eligibility.errors.any?
    #error_explanation
      %h2
        = pluralize(@snap_eligibility.errors.count, "error")
        prohibited this snap_eligibility from being saved:
      %ul
        - @snap_eligibility.errors.full_messages.each do |message|
          %li= message
  .field
    = f.label :snap_dependent_no
    %br/
    = f.number_field :snap_dependent_no
  .field
    = f.label :snap_gross_income
    %br/
    = f.text_field :snap_gross_income
  .actions
    = f.submit
